const AnimationScrollHidden = document.querySelectorAll('.inHidden')
const animeRight = document.querySelectorAll('[data-right]')
const animeLeft = document.querySelectorAll('[data-left]')
const animeTop = document.querySelectorAll('[data-top]')
const animeDown = document.querySelectorAll('[data-down]')
const left = 'OnLeft'
const right = 'OnRight'
const top = 'OnTop'
const down = 'OnDown'
const off = 'off'


export const Settings = {
  animeDown,
  animeTop,
  animeRight,
  animeLeft,
  left,
  right,
  top,
  down,
  off,
  AnimationScrollHidden
}